Welcome to AI Tweet Generator!

What does it do?

Takes a simple text prompt and returns an AI generated tweet within 240 characters.

Features:

- Save Tweet - saves a tweet locally in a cookie.
- Delete Tweet - deletes a tweet from interface and from local memory.
- Load saved tweets - display all tweets saved locally.

Future features:

- Tweet directly to your twitter account. 